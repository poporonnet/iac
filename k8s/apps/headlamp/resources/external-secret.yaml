apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: headlamp
  namespace: headlamp
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: headlamp
  data:
    - secretKey: clientID
      remoteRef:
        key: headlamp
        property: oidc_client_id
