apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forgejo
  namespace: forgejo
spec:
  secretName: forgejo-tls
  dnsNames:
    - git.poporon.org
  issuerRef:
    name: cluster-issuer-poporon-org
    kind: ClusterIssuer
